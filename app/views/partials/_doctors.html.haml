%h2 Doctors:
- doctors.each do |doctor|
  %p
    Doctor ID: #{doctor.id}
  %p= doctor.first_name || "Doesn't have a first name"
  %p= doctor.last_name || "Doesn't have a last name"
  = link_to "Add Appointment", new_appointment_path(doctor_id: doctor.id)
